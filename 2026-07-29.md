📋 daily-review  导航 经验 反思 时间线

RmVlZGJhY2sgUmV2aWV3

# 2026-07-29

周三 · 模型全线切换 Agnes 2.5-flash · Hermes 双保险 · atma-shakti 新文档

模型迁移 配置修复 文档创建

3

今日项目

3

日志来源

✅

整体评价

## 🔥 三条最佳经验

今天最值得记住的三件事——有具体场景、可复用。

1 **模型生态全面迁移：从 NVIDIA 到 Agnes 2.5-flash**

运维迁移 已验证

今天完成了多项服务的模型迁移，核心动作是从 NVIDIA 生态统一切换到 Agnes 2.5-flash（.cn 域名）：

- **Claw/Hermes**：域名 .com→.cn，模型 2.0→2.5-flash，双保险改两处（config.yaml + .env）
- **amruta-daily-push**：全线 7 步脚本迁移（step1~step4, alignment, title, cron），base_url 全部指向 agnes.cn
- **atma-shakti**：新建两份基于 F 盘母亲讲话原文的文档

**可复用经验** ：同一天多项目统一迁移时，先确认新端点的兼容性（Agnes .cn 支持 2.5-flash 但不支持 2.0），再逐项目滚动替换。amruta 的 extract_json() 兼容函数可以复用——LLM 输出带 ` ```json ` 包裹时自动提取。

→ Claw / amruta-daily-push / atma-shakti 日志

2 **Hermes 模型切换双保险机制**

配置方法 已验证

Hermes 存在运行时自改 config.yaml 的 model 段的问题，因此采取双保险策略：同时改 config.yaml 和 .env（.env 更稳定，不会被自改覆盖）。

**可复用经验** ：当应用启动时/运行时可能覆写其配置文件时，不要在同一个文件上反复修复——找到更稳定的配置源（如 .env 环境变量），同时改两处以防一方被改。备份保留为 .bak.2026-07-29 和 .bak.2026-07-29-2 两版。

→ Claw 日志

3 **amruta 全流程迁移验证一次性通过**

流水线验证 已验证

amruta-daily-push 从 NVIDIA NVIDIA NIM 各模型换到 Agnes 2.5-flash，涉及 4 个 step 脚本 + Step2.3/2.4/2.5 对齐/标题 + cron，全部完成迁移后全流程验证一次性通过。Step2.4 Prompt 还新增了「不可多对多」规则，防止交叉映射。

**可复用经验** ：大范围迁移时先做好兼容层（extract_json 防格式变化 + key 读取双源），再逐个 step 滚动验证。偶发 503 时重试即可恢复。

→ amruta-daily-push 日志

## ⚠️ 做得不好的事

诚实面对，给出改进方案。

1 **Hermes 先切 2.0-flash 再切 2.5-flash 绕弯**

配置效率

先按用户指示切到 agnes-2.0-flash，很快又被通知要改到 agnes-2.5-flash。如果第一次确认时直接问"最新版本是什么"，可以少改一轮。

**改进方案** ：涉及版本切换时，先问"当前最新可用版本是什么"再执行，避免两步走。

## 🛠️ 今日技能变化

今天更新的技能和自动化配置

Claw config.yaml + .env / amruta-daily-push 全线 7 脚本 / atma-shakti SKILL.md / Step2.4 Prompt

**📝 Claw Hermes 配置**
config.yaml + .env 双保险改为 Agnes 2.5-flash，备份两版

**🦞 amruta-daily-push 全线 7 脚本**
模型迁移 + extract_json() 兼容 + key 读取修复 + 缩进修复 + Step2.4 "不可多对多"规则

**📝 atma-shakti 文档**
新建两份基于母亲讲话原文的文档

## 📋 今日时间线

从日志中提取的关键节点

#### ~上午 Hermes 模型切换 2.0→2.5-flash

Claw 日志

双保险改 config.yaml + .env（域名 .com→.cn，模型 2.0→2.5），备份 2 版

#### ~上午 amruta-daily-push 全线迁移

amruta-daily-push 日志

全线 7 脚本从 NVIDIA 迁移至 Agnes 2.5-flash，base_url + model + key 全部更新，全流程验证一次性通过 ✅

#### 14:30 Step2.4 Prompt 修正

amruta-daily-push 日志

新增「不可多对多」规则到 Step2.4 对齐模式 Prompt A

#### ~全天 atma-shakti 文档创建

atma-shakti 日志

基于 F 盘母亲讲话原文整理两份文档：普通人的 5 样东西 / 高灵性人群的 5 样东西

#### 全天生话评价

日志综合

周三，模型生态大迁移日。没有个人日记记录。

← 返回导航  
daily-review -- 2026-07-29  
由 WorkBuddy 自动化生成 · 23:50 全局复盘
