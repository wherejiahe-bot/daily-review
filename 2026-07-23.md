📋 daily-review  导航 经验 反思 时间线

C2VpbmcgUmV2aWV3

# 2026-07-23

周四 · Hermes主模型切Agnes全链路调试 · 微信提醒格式铁律定型 · 内在探索工作室6Skill上线

Hermes配置 Agnes API 微信协议 iLink 内在探索 咨询技能

12+

今日任务

4

配置修复

✅

整体评价

## 🔥 三条最佳经验

今天最值得记住的三件事——有具体场景、可复用。

1 **Hermes 配 custom provider 的 key 必须放 OPENAI_API_KEY，不是 model.api_key**

Hermes 配置 经验教训 已验证

今天接手将主人电脑端 Hermes 主模型换为 Agnes（agnes-2.0-flash）。通过查 simple_switch.py 的历史成功经验发现正确写法是 `.env` 的 `model.*` 字段（非 config.yaml）。然而配好后重启报 401 无效令牌。

关键突破：读 Hermes cli.py 第3838-3844 行源码确认——custom provider 只从 `OPENAI_API_KEY` 或 `OPENROUTER_API_KEY` 环境变量读取，**完全不读 `.env` 的 `model.api_key` 字段**。所以送错 token → 401。修复：将 key 写入 `OPENAI_API_KEY`，保留 `model.provider=custom` + `model.base_url` + `model.default` 在 .env。

**可复用经验** ：配 custom provider 时，key 必须放 `OPENAI_API_KEY` 环境变量。`model.api_key` 写法对 Hermes 主程序无效（即使文档这么写）。源码才是真相。同时保留 config.yaml 抄一份 custom provider 配置做双保险（即使 Hermes 运行时会自改 config，.env 的才是稳定生效位）。

2 **微信纯文本通道不能加粗——iLink 协议级铁证**

微信消息 协议 已验证

今早用户看到 `**\u201c一棵树...`——既没加粗（星号字面）又有 \u201c 乱码。深入查 iLink 协议文档发现 `item_list[].type` 只有 1文本/2图片/3语音/4文件/5视频，**无 markdown/富文本型**。文本型只有 `text_item.text` 纯字符串，无字体属性。OpenClaw 官方源码有 `markdownToPlainText()` 把 LLM markdown 转纯文本再发——官方认证无粗体。

**可复用经验** ：微信消息是纯文本通道，`**加粗**` 不会渲染成粗体——只在 WorkBuddy 聊天框 UI 显示为粗体，微信里是字面星号。7 个自动化 prompt 统一改为不用 `**`、不用中文弯引号。真·粗体唯一路径：引语渲染成图片（type:2）发送，需改 send_wechat.py 支持发图+PIL中文字体。

3 **内在探索咨询工作室 6 Skill 从设计方案一次蒸馏完成**

Skill 创建 团队设计 已验证

基于 `G:\Workspace\短视频选题\内在探索咨询工作室-AI团队配置方案.md`，一次性蒸馏创建 6 个 Skill：团队管理器（`inner-explore-team`）+ 5 个角色（罗杰斯人本主义/克里希那穆提灵性督导/德波顿人文内容/卢森堡NVC访客运营/克利尔效率架构）。每个 Skill 含核心思维模型、话术范式、工作流、行为边界。

**可复用经验** ：当设计方案文档已成熟时，将每个角色独立为 Skill 的做法非常清晰——团队成员彼此知晓存在但各自独立运行。团队管理器 Skill 让主人不用记人名，说需求自动匹配角色。

## ⚠️ 做得不好的事

诚实面对，给出改进方案。

1 **Hermes 配 Agnes 耗时过长——源码入手应该更早**

问题定位 效率

从上午开始配 Hermes 主模型为 Agnes，经历了：翻 simple_switch.py → 写 .env 的 model.api_key → 重启报 401 → 查源码确认 key 位置 → 改为 OPENAI_API_KEY 才通。整个过程应在第一次 401 时就**直接读源码查 API key 注入逻辑**，而不是在 .env 字段名上试错。

**改进方案** ：custom provider 的 key 注入方式不确定时，第一动作是读 provider 源码的 env var 读取逻辑，而非查类似项目的历史写法（历史写法可能已过时或不同版本）。本版本 Hermes cli.py:3838-3844 证实现源码只读 OPENAI_API_KEY。

## 🛠️ 今日技能变化

今天更新的技能和自动化配置

内在探索 6Skill Claw 自动化 prompt × 7 MEMORY.md REFLECTION.md

**📝 内在探索咨询工作室 6 Skill 全新创建**

`inner-explore-team`（团队管理器）、`inner-explore-rogers`（人本主义咨询督导）、`inner-explore-krishnamurti`（灵性咨询督导）、`inner-explore-debotton`（人文内容创作）、`inner-explore-rosenberg`（NVC访客运营）、`inner-explore-clear`（效率架构官）。

**📝 Claw 自动化提醒 7 prompt 统一格式修复**

删除 `**` 加粗指示、禁中文全角弯引号（`""`→`""`）、引语用 `【】` 强调。脚本验证 `star_in_fmt:False / curve:False / rule_ok:True` 全通过。

## 📋 今日时间线

从日志中提取的关键节点

#### 全天 ~ Hermes 主模型切 Agnes 全链路调试

Claw-No1 主项目

从找 simple_switch.py 历史成功经验起步，正确 `.env` 写法但 401 → 读 cli.py 源码发现 key 必须放 OPENAI_API_KEY → 配改为 OPENAI_API_KEY 后 OK。保留 config.yaml 双保险。待主人重启 Hermes 验证 footer 显示 agnes-2.0-flash。

#### 全天 ~ 微信提醒格式铁律定型

Claw 工作方式

7 个自动化 prompt 统一修复（启用了脚本核验）。iLink 协议深入调研，产出铁律写 MEMORY.md。确认微信纯文本不能加粗的真·粗体路径是图片方案（待主人拍板）。

#### 全天 ~ 内在探索咨询工作室 6 Skill 上线

知识工程 文档蒸馏

基于设计方案文档一次性蒸馏创建 6 Skill。团队管理器不记人名策略 + 5 角色各有独立 SKILL.md。

#### 全天生活评价

主人自评

（晚间自动复盘，待主人反馈）

← 返回导航  
daily-review -- 2026-07-23  
由 WorkBuddy 自动化生成 · 23:50 全局复盘
