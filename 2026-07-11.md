📋 daily-review  导航 经验 反思 时间线

# 2026-07-11

周六 · 休息日 · amruta 全面审计清理 · 周末只出简版复盘

amruta清理 废弃文件 日期事故修复 daily-review自动化成功

3+

整体评价

## 🔥 三条最佳经验

### 1 **amruta 项目完成全面审计清理**

项目清理

今天对 amruta-daily-push 做了彻底审计：删除了 32 个废弃脚本、2 个测试文件、8 个调试文本、3 个 .bak 备份、旧实验数据目录、__pycache__ 缓存。scripts/ 目录从 47 个文件精简到 15 个。

**可复用经验** ：定期做一次全面审计比"边做边删"效率高得多——批量删除比逐文件决策更快，而且能发现跨文件的引用依赖问题（coordinator.py 38KB 完全无人引用）。

### 2 **6月25日日期错误事故的根本修复**

日期事故 修复经验

`push_input.json` 的 `date` 字段被误写成演讲原始日期 `1995-06-25`，导致 HTML 文件名、articles.json、index.html 卡片、git commit message 全部用了错误年份。今天修复并加了预防检查：Step3 前必检查 `push_input.json` 的 `date` 字段是否以 `2026-` 开头。

**可复用经验** ：自动化流水线中，上游数据的格式/日期错误会级联污染所有下游产出。关键是在上游入口加"断言式检查"而非在下游逐级修复。

### 3 **Step3 git fallback index.html 日期匹配修复**

git fallback 日期修复

修复了 git fallback 更新 index.html 卡片时的日期匹配 bug：pattern 只匹配新日期但旧卡片日期是演讲年份，导致替换失败。改用 `\d{4}-MM-DD` 匹配任意年份。

**可复用经验** ：git fallback 的本质是"踩到 bug 时的 Plan B"，但 Plan B 也可能有 bug。每次 fallback 路径走完后要检查最终产物（index.html 卡片日期）是否正确。

## ⚠️ 做得不好的事

### 1 **6月25日事故的根因是人而非工具**

自动化 人工检查 缺少断言

日期用错演讲原始年份（1995 vs 2026）不是工具 bug，是助手在生成 `push_input.json` 时犯了错误。流水线没有在第一步就加日期格式校验，导致错误一路蔓延到最终产物。

**根因分析** ：流水线设计时假设"上游数据都是对的"，没有在数据入口处做格式断言。

**改进方案** ：Step3 前添加 `push_input.json` 日期字段格式校验（必须 `2026-` 开头），不通过则报错中止。

## 🛠️ 今日技能变化

amruta-daily-push SKILL.md MEMORY.md daily-review自动化 清理

**📝 amruta-daily-push skill**
- 删除重复内容和废弃引用
- 删除脚本清单中的 `daily_pipeline.py` 条目
- 删除 3 个不存在的 references 引用

**📝 MEMORY.md（amruta）**
- 全面精简，删除过时设计缺陷记录
- 删除 multi-agent-team 强制加载描述
- 删除审核流程相关描述
- 保留核心技术决策

## 📋 今日时间线

### 全天 · 休息日 · 项目清理

主人自述

周六休息日。各项目均无新开发任务。

amruta-daily-push 项目做了全面审计清理：删除 47→15 个脚本文件、修复 6月25日日期事故、更新 SKILL.md 和 MEMORY.md。6月25号文章全流程完成（28 个句子对，Step1-4 全部成功）。

### 23:50 每日复盘自动化

自动化

23:50 的每日复盘自动化也正常执行完毕。

← 返回导航  
daily-review -- 2026-07-11  
由 WorkBuddy 自动化生成 · 23:50 全局复盘
