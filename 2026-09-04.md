# 2026-09-04 · 周五

## 主题：象棋AI练棋助手全线升级

---

## 今日核心

### xiangqi-ai 项目 — 深度开发日

**引擎加载修复**（file:// 协议问题）：
- 主人在本地双击打开 HTML（file:// 协议），导致 Web Worker 和 SharedArrayBuffer 被浏览器禁止
- 修复：加 file:// 醒目提示框，Worker 创建包 try/catch，引擎调用安全降级
- 额外发现：之前清理 Edge 进程时把 coop_server 连坐干掉，重启后恢复

**棋盘视觉优化**：
- 棋盘偏左上（上下 padding 不对称），修复 CELL=46, MX=16, MY=23 → 完美居中
- 同步调整棋子圆半径 15.5→19、文字 17→21
- 验证 5 档视口（1280/1024/820/720/580）棋盘饱满、32子全显示

**行棋音效 v8 接入**：
- 换用主人另一项目 xiangqi 里的 v8 音效（"大石块相撞声"）
- 三层带通噪声叠加：石块碰撞主体 + 重量冲击 + 石质脆头
- 吃子版低频压到 140-220Hz，更沉更重
- 删掉 speechSynthesis 人声念"吃"（普通走棋不该念）
- 加 Cache-Control: no-store 防浏览器缓存

**讲解人格系统上线**：
- 新增 chess-systems.js，CHESS_SYSTEMS 数组管理体系话术
- 讲解区加「标准 / 体系」切换，激活后横幅显示体系名 + 红黑 tagline
- 按 8 类局面（主线定型/将军/吃子/车占肋/补士象/士角炮/兵卒/大子机动）输出三段式脚本
- 每类 2-3 个随机变体，slogan 触发概率 22%

**对局收录功能**：
- 自动收录：每步 record(notation)+fens，将死/困毙自动落库
- 📥 收录本局按钮，手动保存进行中的棋
- 列表最近 12 局，localStorage key xq_games_v1
- 点条目回看：◀▶ 步进、谱面同步、回看态走子拦截
- 复制棋谱：回合文本 + 终局 FEN，clipboard 直贴

**棋谱复盘 + 存档**：
- 主人贴 19 回合棋谱（红胜），重放脚本 37 手全合法，终局 FEN 验证 checkmate
- 解答"中炮退而复进"：顿挫布饵，一炮换一车，铁门栓将死
- 存档新约定：games/YYYY-MM-DD-结果-战术名.json + .txt 人类可读版
- 教训：rules.js apply() 不翻 side，复盘必须用 makeMove()

**UX 修复**：
- 音频 bug：res.captured 为空格 `' '`（truthy），导致普通走子永远走吃子分支 → 修成 `res.captured !== ' '`
- 棋盘换石头灰：去黄木配色，石板灰系与暗色 UI 协调

**体系手册精简**（125→66 行）：
- front matter 去 episode 字段
- 红线改：红方优先抢肋道，黑方反宫马盯肋道
- chess-systems.js 精简话术，车占肋分支提前到第 2 类
- 主线引导：systemMainlineMove() 重放历史算"我方已连续走完的主线步数 k"

**异步竞态修复**：
- 旧 analyzePosition 走子期间 old 分析恢复会用旧 best 校验 → 报「非法走法」
- 加 analysisSeq 版本号，走子/悔棋/重置处 ++analysisSeq
- busy 时改 setTimeout(150) 重试自愈而非 return

**难度默认改为「高手」**：
- HTML 按钮 active 从 easy 移到 hard
- JS let difficulty = 'hard'

**深度讲解升级**（坐标级·因果级·战术名）：
- 新增 deepExplain(move, givesCheck, b)：落点语境 + 顿挫识别 + 吃子反吃链 + 弃子信号 + 重炮/绝杀判定
- lines 组装升级：行棋思路 = motive 具体因果 + 「—— 体系视角：」人设句
- 小坑：notation() 在当前局面重算因棋子已移位返回 '?' → 用 toUCI 坐标

**体系误删纠正**（重大教训）：
- 主人说「只需要删第29集这几个字」→ 我误理解为「下架整套体系」
- 彻底纠正：从 backup 恢复手册 + CHESS_SYSTEMS 数据
- 行为准则修正：主人说「去掉 XX 字样」= 只删字样本身，绝不扩大

**真实音效接入**（消除延迟）：
- 根因：AudioContext resume() 异步被阻塞，首次 AI 落子前人类走过一手，ctx 未预创建
- 修复：document 级一次性手势解锁（pointerdown/keydown once:true）
- 预加载真实音效：fetch move1.wav + capture1.wav → decodeAudioData 缓存
- 素材来源：sounddino.com（免费真实录制），ffmpeg 截取前 0.40s/0.50s
- 验证：AI 落子延迟完全同步（wallclock 0.201s vs expected 0.200s）

---

## 做得好的事

- xiangqi-ai 练棋助手从 0 到功能完备（引擎加载/音效/讲解人格/对局收录/棋谱复盘）
- 五条深度讲解：坐标级 + 因果级 + 战术名 + 顿挫识别 + 吃子反吃链
- 主线引导系统：AI 推荐主动体现体系，不再跳着推荐
- 真实音效接入，延迟根因彻底消除（文档级手势解锁 + 预加载）
- 棋盘视觉优化：padding 对称 + 石板灰配色 + 棋子适配
- 对局收录功能完整：自动/手动收录 + 回看 + 复制棋谱

---

## 做得不好的事

- 体系误删：主人说「删第29集字样」→ 我扩大成「下架整套体系」→ 重大教训写入 MEMORY.md 铁律7
- res.captured 空格 truthy bug：未想到空格在 JS 中是 truthy，导致普通走子走吃子音效分支
- 注释提醒：同机 G 盘有 xiangqi（博主复盘）和 xiangqi-ai（练棋助手）两个项目，别混淆

---

## 今日时间线

- 上午：引擎加载修复（file:// 协议）、棋盘视觉优化（CELL/MX/MY 参数）
- 下午：行棋音效 v8 接入、讲解人格系统上线、对局收录功能开发
- 晚间：棋谱复盘 + 存档、UX 修复（音频 bug + 棋盘配色）、体系手册精简
- 深夜：难度默认改高手、深度讲解升级、异步竞态修复、体系误删纠正恢复、真实音效接入

---

## 待办

- 体系手册后续扩展：CHESS_SYSTEMS 框架已好，随时可 push 新体系
- 对局收录：后续可考虑"粘贴导入复现"功能
- 黄庭打卡：检查今日是否完成
